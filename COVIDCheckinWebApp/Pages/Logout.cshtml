@page
@{
    //Sacrificial title so we don't get exceptions
    ViewData["Title"] = "Logout";

    //Clear cookies and redirect
    Response.Cookies.Delete("venueId");
    Response.Redirect("Index");
}